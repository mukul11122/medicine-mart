// SPDX-License-Identifier: MIT
// Copyright (C) 2026 Shogo Technologies, Inc.
/**
 * Auto-generated Client Functions
 *
 * Fetch-based CRUD operations that call the Hono REST API.
 * No server-side imports — safe for browser bundles.
 * Generated by `shogo generate` - DO NOT EDIT
 */

import type {
  UserType,
  UserCreateInput,
  UserUpdateInput,
  CategoryType,
  CategoryCreateInput,
  CategoryUpdateInput,
  MedicineType,
  MedicineCreateInput,
  MedicineUpdateInput,
  AddressType,
  AddressCreateInput,
  AddressUpdateInput,
  CartItemType,
  CartItemCreateInput,
  CartItemUpdateInput,
  OrderType,
  OrderCreateInput,
  OrderUpdateInput,
  OrderItemType,
  OrderItemCreateInput,
  OrderItemUpdateInput,
  PrescriptionType,
  PrescriptionCreateInput,
  PrescriptionUpdateInput,
  WishlistType,
  WishlistCreateInput,
  WishlistUpdateInput,
  CouponType,
  CouponCreateInput,
  CouponUpdateInput,
  OtpVerificationType,
  OtpVerificationCreateInput,
  OtpVerificationUpdateInput,
} from './types'

/** Get the API base URL */
function getApiBase(): string {
  if (typeof window !== 'undefined') {
    return window.location.origin
  }
  return process.env.API_URL || 'http://localhost:3001'
}

// ============================================================================
// User Client Functions
// ============================================================================

/**
 * List all User records
 */
export async function getUserList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<UserType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/users${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list User')
  }
  const json = await response.json()
  return (json.items || []) as UserType[]
}

/**
 * Get a single User by ID
 */
export async function getUserById(args: { data: { id: string; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'User not found')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Create a new User
 */
export async function createUser(args: { data: { input: UserCreateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Update an existing User
 */
export async function updateUser(args: { data: { id: string; input: UserUpdateInput; userId?: string } }): Promise<UserType> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update User')
  }
  const json = await response.json()
  return json.data as UserType
}

/**
 * Delete a User
 */
export async function deleteUser(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/users/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete User')
  }
  return { success: true }
}

// ============================================================================
// Category Client Functions
// ============================================================================

/**
 * List all Category records
 */
export async function getCategoryList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<CategoryType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/categories${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Category')
  }
  const json = await response.json()
  return (json.items || []) as CategoryType[]
}

/**
 * Get a single Category by ID
 */
export async function getCategoryById(args: { data: { id: string; userId?: string } }): Promise<CategoryType> {
  const url = `${getApiBase()}/api/categories/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Category not found')
  }
  const json = await response.json()
  return json.data as CategoryType
}

/**
 * Create a new Category
 */
export async function createCategory(args: { data: { input: CategoryCreateInput; userId?: string } }): Promise<CategoryType> {
  const url = `${getApiBase()}/api/categories`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Category')
  }
  const json = await response.json()
  return json.data as CategoryType
}

/**
 * Update an existing Category
 */
export async function updateCategory(args: { data: { id: string; input: CategoryUpdateInput; userId?: string } }): Promise<CategoryType> {
  const url = `${getApiBase()}/api/categories/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Category')
  }
  const json = await response.json()
  return json.data as CategoryType
}

/**
 * Delete a Category
 */
export async function deleteCategory(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/categories/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Category')
  }
  return { success: true }
}

// ============================================================================
// Medicine Client Functions
// ============================================================================

/**
 * List all Medicine records
 */
export async function getMedicineList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<MedicineType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/medicines${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Medicine')
  }
  const json = await response.json()
  return (json.items || []) as MedicineType[]
}

/**
 * Get a single Medicine by ID
 */
export async function getMedicineById(args: { data: { id: string; userId?: string } }): Promise<MedicineType> {
  const url = `${getApiBase()}/api/medicines/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Medicine not found')
  }
  const json = await response.json()
  return json.data as MedicineType
}

/**
 * Create a new Medicine
 */
export async function createMedicine(args: { data: { input: MedicineCreateInput; userId?: string } }): Promise<MedicineType> {
  const url = `${getApiBase()}/api/medicines`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Medicine')
  }
  const json = await response.json()
  return json.data as MedicineType
}

/**
 * Update an existing Medicine
 */
export async function updateMedicine(args: { data: { id: string; input: MedicineUpdateInput; userId?: string } }): Promise<MedicineType> {
  const url = `${getApiBase()}/api/medicines/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Medicine')
  }
  const json = await response.json()
  return json.data as MedicineType
}

/**
 * Delete a Medicine
 */
export async function deleteMedicine(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/medicines/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Medicine')
  }
  return { success: true }
}

// ============================================================================
// Address Client Functions
// ============================================================================

/**
 * List all Address records
 */
export async function getAddressList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<AddressType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/addresses${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Address')
  }
  const json = await response.json()
  return (json.items || []) as AddressType[]
}

/**
 * Get a single Address by ID
 */
export async function getAddressById(args: { data: { id: string; userId?: string } }): Promise<AddressType> {
  const url = `${getApiBase()}/api/addresses/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Address not found')
  }
  const json = await response.json()
  return json.data as AddressType
}

/**
 * Create a new Address
 */
export async function createAddress(args: { data: { input: AddressCreateInput; userId?: string } }): Promise<AddressType> {
  const url = `${getApiBase()}/api/addresses`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Address')
  }
  const json = await response.json()
  return json.data as AddressType
}

/**
 * Update an existing Address
 */
export async function updateAddress(args: { data: { id: string; input: AddressUpdateInput; userId?: string } }): Promise<AddressType> {
  const url = `${getApiBase()}/api/addresses/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Address')
  }
  const json = await response.json()
  return json.data as AddressType
}

/**
 * Delete a Address
 */
export async function deleteAddress(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/addresses/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Address')
  }
  return { success: true }
}

// ============================================================================
// CartItem Client Functions
// ============================================================================

/**
 * List all CartItem records
 */
export async function getCartItemList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<CartItemType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/cart-items${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list CartItem')
  }
  const json = await response.json()
  return (json.items || []) as CartItemType[]
}

/**
 * Get a single CartItem by ID
 */
export async function getCartItemById(args: { data: { id: string; userId?: string } }): Promise<CartItemType> {
  const url = `${getApiBase()}/api/cart-items/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'CartItem not found')
  }
  const json = await response.json()
  return json.data as CartItemType
}

/**
 * Create a new CartItem
 */
export async function createCartItem(args: { data: { input: CartItemCreateInput; userId?: string } }): Promise<CartItemType> {
  const url = `${getApiBase()}/api/cart-items`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create CartItem')
  }
  const json = await response.json()
  return json.data as CartItemType
}

/**
 * Update an existing CartItem
 */
export async function updateCartItem(args: { data: { id: string; input: CartItemUpdateInput; userId?: string } }): Promise<CartItemType> {
  const url = `${getApiBase()}/api/cart-items/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update CartItem')
  }
  const json = await response.json()
  return json.data as CartItemType
}

/**
 * Delete a CartItem
 */
export async function deleteCartItem(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/cart-items/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete CartItem')
  }
  return { success: true }
}

// ============================================================================
// Order Client Functions
// ============================================================================

/**
 * List all Order records
 */
export async function getOrderList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<OrderType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/orders${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Order')
  }
  const json = await response.json()
  return (json.items || []) as OrderType[]
}

/**
 * Get a single Order by ID
 */
export async function getOrderById(args: { data: { id: string; userId?: string } }): Promise<OrderType> {
  const url = `${getApiBase()}/api/orders/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Order not found')
  }
  const json = await response.json()
  return json.data as OrderType
}

/**
 * Create a new Order
 */
export async function createOrder(args: { data: { input: OrderCreateInput; userId?: string } }): Promise<OrderType> {
  const url = `${getApiBase()}/api/orders`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Order')
  }
  const json = await response.json()
  return json.data as OrderType
}

/**
 * Update an existing Order
 */
export async function updateOrder(args: { data: { id: string; input: OrderUpdateInput; userId?: string } }): Promise<OrderType> {
  const url = `${getApiBase()}/api/orders/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Order')
  }
  const json = await response.json()
  return json.data as OrderType
}

/**
 * Delete a Order
 */
export async function deleteOrder(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/orders/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Order')
  }
  return { success: true }
}

// ============================================================================
// OrderItem Client Functions
// ============================================================================

/**
 * List all OrderItem records
 */
export async function getOrderItemList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<OrderItemType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/order-items${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list OrderItem')
  }
  const json = await response.json()
  return (json.items || []) as OrderItemType[]
}

/**
 * Get a single OrderItem by ID
 */
export async function getOrderItemById(args: { data: { id: string; userId?: string } }): Promise<OrderItemType> {
  const url = `${getApiBase()}/api/order-items/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'OrderItem not found')
  }
  const json = await response.json()
  return json.data as OrderItemType
}

/**
 * Create a new OrderItem
 */
export async function createOrderItem(args: { data: { input: OrderItemCreateInput; userId?: string } }): Promise<OrderItemType> {
  const url = `${getApiBase()}/api/order-items`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create OrderItem')
  }
  const json = await response.json()
  return json.data as OrderItemType
}

/**
 * Update an existing OrderItem
 */
export async function updateOrderItem(args: { data: { id: string; input: OrderItemUpdateInput; userId?: string } }): Promise<OrderItemType> {
  const url = `${getApiBase()}/api/order-items/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update OrderItem')
  }
  const json = await response.json()
  return json.data as OrderItemType
}

/**
 * Delete a OrderItem
 */
export async function deleteOrderItem(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/order-items/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete OrderItem')
  }
  return { success: true }
}

// ============================================================================
// Prescription Client Functions
// ============================================================================

/**
 * List all Prescription records
 */
export async function getPrescriptionList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<PrescriptionType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/prescriptions${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Prescription')
  }
  const json = await response.json()
  return (json.items || []) as PrescriptionType[]
}

/**
 * Get a single Prescription by ID
 */
export async function getPrescriptionById(args: { data: { id: string; userId?: string } }): Promise<PrescriptionType> {
  const url = `${getApiBase()}/api/prescriptions/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Prescription not found')
  }
  const json = await response.json()
  return json.data as PrescriptionType
}

/**
 * Create a new Prescription
 */
export async function createPrescription(args: { data: { input: PrescriptionCreateInput; userId?: string } }): Promise<PrescriptionType> {
  const url = `${getApiBase()}/api/prescriptions`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Prescription')
  }
  const json = await response.json()
  return json.data as PrescriptionType
}

/**
 * Update an existing Prescription
 */
export async function updatePrescription(args: { data: { id: string; input: PrescriptionUpdateInput; userId?: string } }): Promise<PrescriptionType> {
  const url = `${getApiBase()}/api/prescriptions/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Prescription')
  }
  const json = await response.json()
  return json.data as PrescriptionType
}

/**
 * Delete a Prescription
 */
export async function deletePrescription(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/prescriptions/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Prescription')
  }
  return { success: true }
}

// ============================================================================
// Wishlist Client Functions
// ============================================================================

/**
 * List all Wishlist records
 */
export async function getWishlistList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<WishlistType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/wishlists${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Wishlist')
  }
  const json = await response.json()
  return (json.items || []) as WishlistType[]
}

/**
 * Get a single Wishlist by ID
 */
export async function getWishlistById(args: { data: { id: string; userId?: string } }): Promise<WishlistType> {
  const url = `${getApiBase()}/api/wishlists/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Wishlist not found')
  }
  const json = await response.json()
  return json.data as WishlistType
}

/**
 * Create a new Wishlist
 */
export async function createWishlist(args: { data: { input: WishlistCreateInput; userId?: string } }): Promise<WishlistType> {
  const url = `${getApiBase()}/api/wishlists`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Wishlist')
  }
  const json = await response.json()
  return json.data as WishlistType
}

/**
 * Update an existing Wishlist
 */
export async function updateWishlist(args: { data: { id: string; input: WishlistUpdateInput; userId?: string } }): Promise<WishlistType> {
  const url = `${getApiBase()}/api/wishlists/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Wishlist')
  }
  const json = await response.json()
  return json.data as WishlistType
}

/**
 * Delete a Wishlist
 */
export async function deleteWishlist(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/wishlists/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Wishlist')
  }
  return { success: true }
}

// ============================================================================
// Coupon Client Functions
// ============================================================================

/**
 * List all Coupon records
 */
export async function getCouponList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<CouponType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/coupons${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list Coupon')
  }
  const json = await response.json()
  return (json.items || []) as CouponType[]
}

/**
 * Get a single Coupon by ID
 */
export async function getCouponById(args: { data: { id: string; userId?: string } }): Promise<CouponType> {
  const url = `${getApiBase()}/api/coupons/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Coupon not found')
  }
  const json = await response.json()
  return json.data as CouponType
}

/**
 * Create a new Coupon
 */
export async function createCoupon(args: { data: { input: CouponCreateInput; userId?: string } }): Promise<CouponType> {
  const url = `${getApiBase()}/api/coupons`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create Coupon')
  }
  const json = await response.json()
  return json.data as CouponType
}

/**
 * Update an existing Coupon
 */
export async function updateCoupon(args: { data: { id: string; input: CouponUpdateInput; userId?: string } }): Promise<CouponType> {
  const url = `${getApiBase()}/api/coupons/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update Coupon')
  }
  const json = await response.json()
  return json.data as CouponType
}

/**
 * Delete a Coupon
 */
export async function deleteCoupon(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/coupons/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete Coupon')
  }
  return { success: true }
}

// ============================================================================
// OtpVerification Client Functions
// ============================================================================

/**
 * List all OtpVerification records
 */
export async function getOtpVerificationList(args: { data: { userId?: string; where?: Record<string, unknown> } }): Promise<OtpVerificationType[]> {
  const params = new URLSearchParams()
  if (args.data.userId) params.set('userId', args.data.userId)
  if (args.data.where) {
    for (const [key, value] of Object.entries(args.data.where)) {
      if (value !== undefined && value !== null) params.set(key, String(value))
    }
  }
  const qs = params.toString()
  const url = `${getApiBase()}/api/otp-verifications${qs ? `?${qs}` : ''}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to list OtpVerification')
  }
  const json = await response.json()
  return (json.items || []) as OtpVerificationType[]
}

/**
 * Get a single OtpVerification by ID
 */
export async function getOtpVerificationById(args: { data: { id: string; userId?: string } }): Promise<OtpVerificationType> {
  const url = `${getApiBase()}/api/otp-verifications/${args.data.id}`
  const response = await fetch(url)
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'OtpVerification not found')
  }
  const json = await response.json()
  return json.data as OtpVerificationType
}

/**
 * Create a new OtpVerification
 */
export async function createOtpVerification(args: { data: { input: OtpVerificationCreateInput; userId?: string } }): Promise<OtpVerificationType> {
  const url = `${getApiBase()}/api/otp-verifications`
  const response = await fetch(url, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to create OtpVerification')
  }
  const json = await response.json()
  return json.data as OtpVerificationType
}

/**
 * Update an existing OtpVerification
 */
export async function updateOtpVerification(args: { data: { id: string; input: OtpVerificationUpdateInput; userId?: string } }): Promise<OtpVerificationType> {
  const url = `${getApiBase()}/api/otp-verifications/${args.data.id}`
  const response = await fetch(url, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(args.data.input),
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to update OtpVerification')
  }
  const json = await response.json()
  return json.data as OtpVerificationType
}

/**
 * Delete a OtpVerification
 */
export async function deleteOtpVerification(args: { data: { id: string; userId?: string } }): Promise<{ success: boolean }> {
  const url = `${getApiBase()}/api/otp-verifications/${args.data.id}`
  const response = await fetch(url, {
    method: 'DELETE',
  })
  if (!response.ok) {
    const err = await response.json().catch(() => ({ error: { message: response.statusText } }))
    throw new Error(err.error?.message || 'Failed to delete OtpVerification')
  }
  return { success: true }
}
